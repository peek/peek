<% if Glimpse.enabled? %>
<div id="glimpse">
  <div class="wrapper">
    <% Glimpse.views.each do |view| %>
      <%= render view.partial_path, :view => view %>
    <% end %>
  </div>
</div>
<% end %>
